#include <Arduino.h>

/*
// Initialize the GPS module
*/
void initGPS();

/*
// Request the Latitude value, return the requested value as a String
*/
String requestGPSLat();

/*
// Request the Longitude value, return the requested value as a String
*/
String requestGPSLng();